<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yhl.dao.BaseDao">

    <sql id="Base_Column_List">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Aug 22 15:24:50 CST 2018.
        -->
        region_code, region_name, country_code, country_name, province_code, province_name,
        city_code, city_name, area_code, area_name, township_code, township_name, village_code,
        village_name, create_time, update_time, layer_class, mome, latitude, longitude, scort,
        acreage
    </sql>


    <select id="selectCityAndProvince" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from
        `dim_region`
    </select>

    <select id="findCityByProvinceCode" resultType="java.util.Map">
		SELECT DISTINCT city_name,city_code from dim_region
	 where province_code=#{provinceCode}
	 and province_name !=region_name
    </select>
    	
</mapper>